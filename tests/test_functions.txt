# Test file for range functions

# Set up initial values
A1=10
B1=20
C1=30
D1=40
E1=50

# SUM function
A2=SUM(A1:E1)
B2=SUM(A1:A1)
C2=SUM(A1:C1)

# AVG function
D2=AVG(A1:E1)
E2=AVG(A1:A1)
F1=AVG(A1:C1)

# MIN function
A3=MIN(A1:E1)
B3=MIN(A1:A1)
C3=MIN(C1:E1)

# MAX function
D3=MAX(A1:E1)
E3=MAX(A1:A1)
F2=MAX(C1:E1)

# STDEV function
A4=STDEV(A1:E1)
B4=STDEV(A1:A1)  # Should be ERROR_VALUE (need at least 2 values)
C4=STDEV(A1:C1)

# Invalid ranges
D4=SUM(E1:A1)  # End before start
E4=MIN(Z1:Z5)  # Out of initialized range

# SLEEP function
A5=SLEEP(1)
B5=SLEEP(0)

# Exit
q 